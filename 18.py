class A:
    def __init__(self,name,count):
        self.count = count
        self.name = name
    def add(self):
        a = open("self.name" + ".txt",a)
        a.white("存入" + self.count)
        a.close()
    def self(self):
        a = open("self.name" + ".txt",a)
        a.white("卖出" + self.count)
        a.close()
